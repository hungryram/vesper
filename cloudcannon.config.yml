collections_config:
  testimonials:
    _enabled_editors:
      - data
      - visual
    disable_add: true
  thank-you:
    _enabled_editors:
      - visual
    disable_add: true

paths:
  # The default location for newly uploaded files, relative to source
  uploads: assets